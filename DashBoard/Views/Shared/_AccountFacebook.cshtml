@* @using Dashboard.DataDto.ViewModels;

@model List<AccountFbViewModels>

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Danh sách tài khoản Facebook</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>Tên tài khoản</th>
                    <th>Mật khẩu</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.FbUser</td>
                        <td>@item.FbPassword</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
 *@